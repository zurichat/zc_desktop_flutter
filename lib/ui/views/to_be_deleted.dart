import 'package:flutter/material.dart';

class ToBeDeleted extends StatelessWidget {
  const ToBeDeleted({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Hello World'),
    );
  }
}