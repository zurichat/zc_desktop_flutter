import 'package:flutter/material.dart';

class ChannelSettingTab extends StatelessWidget {
  const ChannelSettingTab({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
       color: Colors.grey[100],
    );
  }
}