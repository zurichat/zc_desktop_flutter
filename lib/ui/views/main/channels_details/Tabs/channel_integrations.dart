import 'package:flutter/material.dart';

class ChannelIntegrationTab extends StatelessWidget {
  const ChannelIntegrationTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
       color: Colors.grey[100],
    );
  }
}
